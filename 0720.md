# Intervals

### Checking if two intervals overlap:

```python
def is_overlap(a,b):
    return (a[0]<b[1] and b[0]<a[1])
```

### merging two intervals:

```python
def mergeIntervals(a,b):
    return ([min(a[0],b[0],max(a[1],b1[1])])
```

# Sort Functions

https://ooyoung.tistory.com/59

num_list = [15, 22, 8, 79, 10]

sort

```python
num_list.sort()
print(num_list) #[8, 10, 15, 22, 79]
```

sorted

```python
sortedVals = sorted(num_list)
print(num_list) #[15, 22, 8, 79, 10]
print(sortedVals) #[8, 10, 15, 22, 79]
```

### Parameters

1. reverse

```python
num_list.sort(reverse = True)
print(num_list) #[79,22,15,10,8]
```

```python
print(sorted(['좋은하루','good_morning','굿모닝','niceday'], reverse=True))
# ['좋은하루', '굿모닝', 'niceday', 'good_morning']
```

2. key

- len
  ```python
  str_list = ['좋은하루','good_morning','굿모닝','niceday']
  print(sorted(str_list, key=len))  # 함수
    #['굿모닝', '좋은하루', 'niceday', 'good_morning']
  ```
- lambda

  ```python
  print(sorted(str_list, key=lambda x : x[1]))  # 람다
    #['niceday', 'good_morning', '굿모닝', '좋은하루']
  ```

  ```python
  >>> tuple_list = [('좋은하루', 0),
                  ('niceday', 1),
                  ('좋은하루', 5),
                  ('good_morning', 3),
                  ('niceday',5)]

  >>> tuple_list.sort(key=lambda x : (x[0], x[1]))  # '-'부호를 이용해서 역순으로 가능
  >>> print(tuple_list)
  [('good_morning', 3), ('niceday', 1), ('niceday', 5), ('좋은하루', 0), ('좋은하루', 5)]
  ```
