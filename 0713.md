### Linked List

```python
class Node:
    def __init__(self,value):
        self.value = value
        self.next = None

class LinkedList:
    def __init__(self):
        self.head = None

    def __len__(self):
        current = self.head
        count = 0
        while current:
            count += 1
            current = current.next
        return count

    def append(self,node):
        if self.head == None:
            self.head = node
            return
        current = self.head
        while current.next != None:
            current = current.next
        current.next = node

    def addFirst(self,node):
        current = self.head
        node.next = current
        self.head = node

    def clear(self):
        self.head = None

    def remove(self,val):
        dummyNode = Node("dummy")
        dummyNode.next = self.head
        self.head  = dummyNode
        current = self.head
        nextVal = current.next

        while nextVal:
            if nextVal.value == val:
                current.next = nextVal.next
            current = current.next
            nextVal = nextVal.next

        self.head = self.head.next


    def contains(self,num):
        current = self.head
        while current:
            if current.value == num:
                return True
            current = current.next
        return False

    def pop(self):
        current = self.head
        if current == None:
            return
        if current.next == None:
            self.head = None
            return
        while current.next.next != None:
            current = current.next
        current.next = None



    def __str__(self):
        current = self.head
        linkedList = ""
        while current:
            linkedList += f"{current.value} -> "
            current = current.next
        return linkedList
```

### Floyd's Tortoise and Hare AKA Cycle Detection Algorithm

- moving two pointers at different speeds through the sequence of values until they both point to equal values.
- Useful when detecting a cycle in a linked list.

```python
def hasCycle(self, head: Optional[ListNode]) -> bool:
    slow,fast = head,head
    while fast and fast.next:
        slow = slow.next
        fast = fast.next.next
        if slow == fast:
            return True
    return False
```

### Stack

- list implementation

```python
class Stack():
    def __init__(self):
        self.stack = []

    def push(self,val):
        self.stack.append(val)

    def pop(self):
        if len(self.stack)>1:
            self.stack.pop()
            return
        print("Stack is Empty")

    def isEmpty(self):
        return True if len(self.stack) == 0 else False

    def search(self,val):
        for (index,value) in self.stack:
            if value == val:
                return index
        return -1

    def peek(self):
        if len(self.stack) > 1:
            return (self.stack[-1])
        return -1
    def __str__(self):
        return(f"{self.stack}")
```

- linked list implementation

```python
class Node:
    def __init__(self,val):
        self.val = val
        self.next = None

class Stack:
    def __init__(self):
        self.head = None

    def push(self,node):
        current = self.head
        if not current:
            self.head = node
            return
        while current.next:
            current = current.next
        current.next = node

    def pop(self):
        current = self.head
        if not current:
            return -1
        if not current.next:
            self.head = None
            return
        while current.next.next:
            current = current.next
        current.next = None

    def isEmpty(self):
        return True if self.head else False

    def search(self,val):
        current = self.head
        while current:
            if current.val == val:
                return True
            current = current.next
        return False

    def __str__(self):
        current = self.head
        res = ""
        while current:
            res += f"{current.val} -> "
            current = current.next
        return res

```

### Queue

- list implementation

```python
class Queue():
    def __init__(self):
        self.queue = []

    def enqueue(self,val):
        self.queue.insert(0,val)

    def dequeue(self):
        if len(self.queue) > 1 :
            self.queue.pop()

    def front(self):
        return (self.queue[0]) if len(self.queue) > 1  else -1

    def back(self):
        return (self.queue[-1]) if len(self.queue) > 1 else -1

    def isEmpty(self):
        return len(self.queue)>1

        return (f"{self.queue}")
    def __str__(self):
```

- class implementation

```python
class Node():
    def __init__(self,val):
        self.val = val
        self.next = None

class Queue():
    def __init__(self):
        self.head = None

    def enqueue(self,node):
        node.next = self.head
        self.head = node

    def dequeue(self):
        current = self.head
        if not current:
            return
        if current.next == None:
            self.head = None
            return
        while current.next.next:
            current = current.next
        current.next = None

    def front(self):
        return self.head.val if self.head else "does not exist"

    def back(self):
        current = self.head
        if not current:
            return "does not exist"
        while current.next:
            current = current.next
        return current.val

    def __str__(self):
        current = self.head
        res = ""
        while current:
            res += f"{current.val} -> "
            current = current.next
        return res
```
